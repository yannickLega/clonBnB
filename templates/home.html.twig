{% extends 'base.html.twig' %} 

{% block title %}Bienvenue sur la location d'appart qui déchire {% endblock %}

{% block body %}
    <div class="container">
        <div class="jumbotron mt-3">
            <h1 class="display-3">Bienvenue sur CloneBnB</h1>
            <p class="lead">Nous mettons en relation, les voyageurs qui souhaitent faire des économies avec les propriétaires qui veulent gagner de l'argent, en proposant leur(s) bien(s) à la location.</p>
            <hr class="my-4">
            <p>Vous êtes un voyageur ? Vous voulez savoir où vous pourrez loger à votre prochaine destination? Si ce n'est pas sur une autre planète, alors, vous trouverez votre bonheur ici !!!</p>
            <p class="lead">
                <a class="btn btn-primary btn-lg" href="{{ path('ads_index') }}" role="button">Voir les annonces</a>
            </p>
        </div>

        <h2 class="h1 my-5">Nos appartements stars</h2>

        <div class="row">
            {% for data in ads %}
                {% include 'ad/_ad.html.twig' with {'ad': data.annonce} %}
            {% endfor %}
        </div>

        <h2 class="h1 my-5">Nos propriétaires stars</h2>

        <div class="row">
            {% for data in users %}
                <div class="col">
                    <div class="card mb-3">
                        <div class="card-body">
                            <h4 class="card-title">{{data.user.fullName}}</h4>
                            <hr>
                            <div class="card-text">
                                <img src="{{data.user.picture}}" alt="avatar de {{data.user.fullName}}" class="float-left mr-3">
                                <p>{{data.user.introduction}}</p>
                                <a href="{{ path('user_show', {'slug': data.user.slug}) }}" class="btn btn-success float-right">En savoir plus</a>
                            </div>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>
{% endblock %}